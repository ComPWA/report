[project]
channels = ["conda-forge"]
name = "compwa-report"
platforms = ["linux-64"]

[activation]
env = {PYTHONHASHSEED = "0"}
scripts = ["docs/install-julia.sh"]

[dependencies]
graphviz = "*"
juliaup = "*"
uv = "*"
