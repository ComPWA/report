[project]
name = "technical-report"
version = "0.1.0"
requires-python = "~=3.13.0"
dependencies = [
    "ampform-dpd~=0.2.3",
    "ipykernel",
    "matplotlib",
    "numpy",
    "qrules",
    "tensorwaves",
]

[tool.uv.workspace]
